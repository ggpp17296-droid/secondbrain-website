# 过程记录

## 2026-06-15

- 操作：按全局 SOP 创建 `project-004-secondbrain-demopage` 项目骨架。
- 决策：已有最大项目编号为 `003`，新项目使用编号 `004`。
- 文件：创建标准文档和 `assets/`、`data/`、`drafts/`、`outputs/`、`src/`、`tests/` 目录。
- 验证：待同步到全局项目目录后检查。

- 操作：执行阶段 1 技术架构搭建，使用 `create-next-app` 在 `src/` 创建 Next.js 工程。
- 决策：按开发指南采用 App Router、TypeScript、Tailwind CSS、`@/*` 路径别名；删除脚手架生成的嵌套 `src/.git`，避免影响项目根目录统一管理。
- 文件：新增 `/src` Next.js 工程文件、5 个基础路由页面、`scripts/vault-sync.js`、同步生成的 `data/*.json`。
- 验证：`node scripts/vault-sync.js`、`npm run lint`、`npm run build` 均通过；浏览器检查 `/`、`/learning`、`/projects`、`/knowledge`、`/about` 均可渲染且无控制台错误。

- 操作：按 `docs/05_设计系统_Linear风格.md` 与 `docs/06_风格调整执行指南.md` 应用 Linear 风格。
- 决策：保留已有 Next.js App Router 结构，补充 Inter 字体、设计系统 CSS、Tailwind token、通用 UI 组件、毛玻璃 Header、移动端折叠菜单和首页 Linear 风格首屏。
- 文件：更新 `src/src/app/globals.css`、`src/src/app/layout.tsx`、5 个路由页面、`src/src/app/design-system.css`；新增 `src/tailwind.config.js`、`src/src/components/ui/*`、`src/src/components/layout/Header.tsx`。
- 验证：`npm run lint`、`npm run build` 均通过；浏览器验证桌面和 390px 移动视口无横向溢出，Header 毛玻璃为 `blur(16px)`，主题切换生效，5 个路由均可渲染且无 error overlay。

- 操作：执行 `docs/07_阶段2剩余开发任务.md`，完成剩余 3 个页面：学习仪表盘、项目展示、知识网络。
- 决策：新增服务端数据读取模块 `src/src/lib/vault-data.ts`，用 `fs` 从项目根目录 `data/` 读取真实 JSON，避免 Next/Turbopack 直接 import app 源码外层 JSON 的构建限制；知识网络只把筛选和详情状态放入客户端组件。
- 文件：更新 `src/src/app/learning/page.tsx`、`src/src/app/projects/page.tsx`、`src/src/app/knowledge/page.tsx`、`src/src/components/layout/Header.tsx`；新增 `src/src/lib/vault-data.ts`、`src/src/components/knowledge/KnowledgeExplorer.tsx`。
- 验证：每完成一个页面后运行 `npm run build`，三次均通过；最终 `npm run build` 与 `npm run lint` 均通过。使用 Browser 在 `http://localhost:3001` 生产预览验证 `/learning`、`/projects`、`/knowledge`，确认页面内容、知识类型筛选、卡片详情和移动端 390px 布局正常，无横向溢出、无 error overlay。

- 操作：检查知识网络页的思维导图功能。
- 决策：发现 Markmap SVG 因父级只有 `min-height` 而没有确定高度，实际渲染高度退回到默认 150px；在思维导图 Card 内增加明确高度 wrapper，让 SVG 能占满导图面板。
- 文件：更新 `src/src/components/knowledge/KnowledgeView.tsx`。
- 验证：`npm run lint`、`npm run build` 均通过；重启 `http://localhost:3001` 后用 Browser 验证“思维导图”切换，桌面 SVG 为 1230×600，移动端 SVG 为 340×520，节点/连线正常生成，无相关控制台错误。

## 2026-06-16

- 操作：执行阶段 4 部署与自动化，初始化根目录 Git 仓库，创建公开 GitHub 仓库 `ggpp17296-droid/secondbrain-website` 并推送 `main`。
- 决策：Vercel 项目从 `src/` 目录部署；GitHub Actions 使用私有 Vault 仓库 `ggpp17296-droid/my-brain` 作为同步来源，每天北京时间 10:00 运行，也支持手动和 repository dispatch 触发。
- 文件：新增根目录 `.gitignore`、`scripts/package.json`、`scripts/package-lock.json`、`.github/workflows/daily-sync.yml`；同步生成 `data/*.json`。
- 验证：`VAULT_PATH=/Users/jacklee/.claude/workspace/my-brain OUTPUT_PATH=./data node scripts/vault-sync.js`、`npm run lint`、`npm run build` 均通过；首次 Vercel 部署发现 `/about` 预渲染读取 `/vercel/data/vault-data.json` 失败。
- 操作：修复 Vercel 构建期数据路径问题。
- 决策：将构建期数据镜像到 Next.js 项目根内的 `src/data/`，页面读取 `process.cwd()/data`，避免依赖部署目录外层的 `../data`；workflow 同步后同时提交 `data/` 与 `src/data/`，并在配置 `VERCEL_TOKEN` 后通过 Vercel CLI 从 `src/` 目录生产部署。
- 文件：更新 `.github/workflows/daily-sync.yml`、`src/src/lib/vault-data.ts`；新增 `src/data/*.json`、`src/src/lib/vault-data.test.mjs`。
- 验证：先运行路径测试确认缺少 `getDataFilePath` 失败，再实现修复；之后 `node --experimental-strip-types --no-warnings --test src/lib/vault-data.test.mjs`、workflow YAML 解析、`npm run lint`、`npm run build` 均通过。
