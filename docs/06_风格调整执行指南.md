# 风格调整实施指南

## 给 Codex 的执行说明

---

## 📋 任务概述

将现有的 Next.js 项目应用 Linear 风格设计系统，提升视觉效果。

**目标**：从"单调"变成"现代科技感 + 精美"

**参考**：Linear.app 的设计风格

**时间估计**：2-3 小时

---

## 🎯 执行步骤

### 步骤 1：引入 Inter 字体（10 分钟）

**位置**：`src/src/app/layout.tsx`

**操作**：
1. 从 `next/font/google` 导入 Inter 字体
2. 应用到 `<html>` 标签

**代码示例**：
```typescript
import { Inter } from 'next/font/google'

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN" className={inter.variable}>
      <body>{children}</body>
    </html>
  )
}
```

---

### 步骤 2：应用设计系统 CSS（15 分钟）

**位置**：`src/src/app/globals.css`

**操作**：
1. 读取 `src/src/app/design-system.css` 的内容
2. 替换当前 `globals.css` 的内容（保留 Tailwind 指令）

**最终 globals.css 结构**：
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 然后是 design-system.css 的所有内容 */
:root {
  --primary: #5E6AD2;
  /* ... */
}
```

---

### 步骤 3：更新 Tailwind 配置（10 分钟）

**位置**：`tailwind.config.js` 或 `tailwind.config.ts`

**操作**：添加自定义颜色、字体等配置

**代码**：
```javascript
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
  darkMode: ['class', '[data-theme="dark"]'],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#5E6AD2',
          light: '#8B5CF6',
          dark: '#4C51BF',
        },
        accent: {
          DEFAULT: '#06B6D4',
          light: '#22D3EE',
        },
        success: '#10B981',
        warning: '#F59E0B',
        error: '#EF4444',
        dark: {
          bg: '#0A0B0F',
          elevated: '#16171D',
          hover: '#1F2028',
        },
      },
      fontFamily: {
        sans: ['var(--font-inter)', 'Inter', 'sans-serif'],
      },
      borderRadius: {
        card: '12px',
      },
      boxShadow: {
        'card': '0 4px 12px rgba(0, 0, 0, 0.4)',
        'card-hover': '0 8px 24px rgba(0, 0, 0, 0.3)',
        'primary': '0 8px 24px rgba(94, 106, 210, 0.3)',
      },
      backdropBlur: {
        'glass': '16px',
      },
    },
  },
  plugins: [],
}
```

---

### 步骤 4：创建通用组件（30 分钟）

#### 4.1 按钮组件

**位置**：`src/src/components/ui/Button.tsx`

**代码**：
```typescript
import React from 'react'

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost'
  size?: 'sm' | 'md' | 'lg'
  children: React.ReactNode
}

export function Button({ 
  variant = 'primary', 
  size = 'md',
  className = '',
  children,
  ...props 
}: ButtonProps) {
  const baseStyles = 'inline-flex items-center justify-center font-medium rounded-lg transition-all duration-200 ease-out disabled:opacity-50 disabled:cursor-not-allowed'
  
  const variants = {
    primary: 'bg-gradient-to-br from-primary to-primary-light text-white hover:-translate-y-0.5 hover:shadow-primary',
    secondary: 'bg-dark-elevated text-text-primary border border-border-default hover:bg-dark-hover hover:border-border-strong',
    ghost: 'text-text-secondary hover:text-text-primary hover:bg-dark-elevated',
  }
  
  const sizes = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-6 py-3 text-base',
    lg: 'px-8 py-4 text-lg',
  }
  
  return (
    <button
      className={`${baseStyles} ${variants[variant]} ${sizes[size]} ${className}`}
      {...props}
    >
      {children}
    </button>
  )
}
```

#### 4.2 卡片组件

**位置**：`src/src/components/ui/Card.tsx`

**代码**：
```typescript
import React from 'react'

interface CardProps {
  children: React.ReactNode
  className?: string
  hover?: boolean
  glass?: boolean
}

export function Card({ 
  children, 
  className = '', 
  hover = true,
  glass = false 
}: CardProps) {
  const baseStyles = 'rounded-card border transition-all duration-200 ease-out'
  const hoverStyles = hover ? 'hover:-translate-y-1 hover:shadow-card-hover' : ''
  const glassStyles = glass 
    ? 'bg-glass-bg backdrop-blur-glass border-glass-border' 
    : 'bg-dark-elevated border-border-default shadow-card'
  
  return (
    <div className={`${baseStyles} ${hoverStyles} ${glassStyles} ${className}`}>
      {children}
    </div>
  )
}
```

#### 4.3 进度条组件

**位置**：`src/src/components/ui/ProgressBar.tsx`

**代码**：
```typescript
import React from 'react'

interface ProgressBarProps {
  value: number
  max?: number
  label?: string
  showPercentage?: boolean
  size?: 'sm' | 'md' | 'lg'
}

export function ProgressBar({ 
  value, 
  max = 100, 
  label,
  showPercentage = true,
  size = 'md'
}: ProgressBarProps) {
  const percentage = Math.round((value / max) * 100)
  
  const heights = {
    sm: 'h-1',
    md: 'h-2',
    lg: 'h-3',
  }
  
  return (
    <div className="space-y-2">
      {(label || showPercentage) && (
        <div className="flex items-center justify-between text-sm">
          {label && <span className="text-text-secondary">{label}</span>}
          {showPercentage && (
            <span className="text-text-primary font-medium">{percentage}%</span>
          )}
        </div>
      )}
      <div className={`w-full bg-dark-elevated rounded-full overflow-hidden ${heights[size]}`}>
        <div 
          className="h-full bg-gradient-to-r from-primary to-primary-light transition-all duration-500 ease-out rounded-full"
          style={{ width: `${percentage}%` }}
        />
      </div>
    </div>
  )
}
```

#### 4.4 徽章组件

**位置**：`src/src/components/ui/Badge.tsx`

**代码**：
```typescript
import React from 'react'

interface BadgeProps {
  children: React.ReactNode
  variant?: 'success' | 'warning' | 'error' | 'info' | 'pending'
  size?: 'sm' | 'md'
}

export function Badge({ 
  children, 
  variant = 'info',
  size = 'sm' 
}: BadgeProps) {
  const variants = {
    success: 'bg-success/10 text-success',
    warning: 'bg-warning/10 text-warning',
    error: 'bg-error/10 text-error',
    info: 'bg-info/10 text-info',
    pending: 'bg-pending/10 text-pending',
  }
  
  const sizes = {
    sm: 'px-2 py-0.5 text-xs',
    md: 'px-3 py-1 text-sm',
  }
  
  return (
    <span className={`inline-flex items-center rounded-full font-medium ${variants[variant]} ${sizes[size]}`}>
      {children}
    </span>
  )
}
```

---

### 步骤 5：更新首页（30 分钟）

**位置**：`src/src/app/page.tsx`

**操作**：应用新的设计系统，添加渐变、卡片、动画

**代码示例**：
```typescript
import { Button } from '@/components/ui/Button'
import { Card } from '@/components/ui/Card'
import { ProgressBar } from '@/components/ui/ProgressBar'
import { Badge } from '@/components/ui/Badge'

export default function HomePage() {
  return (
    <div className="min-h-screen">
      {/* Hero 区域 */}
      <section className="relative overflow-hidden">
        {/* 渐变背景 */}
        <div className="absolute inset-0 bg-gradient-to-b from-dark-bg via-dark-elevated to-dark-bg" />
        
        {/* 内容 */}
        <div className="relative max-w-7xl mx-auto px-6 py-24">
          <div className="text-center space-y-6">
            <Badge variant="info">第二大脑展示页</Badge>
            <h1 className="text-6xl font-bold">
              <span className="text-gradient">从房地产到 AI</span>
              <br />
              <span className="text-text-primary">的 5 年转型之旅</span>
            </h1>
            <p className="text-xl text-text-secondary max-w-2xl mx-auto">
              这里记录我的学习历程、项目进展和知识沉淀
            </p>
            
            {/* 进度展示 */}
            <div className="max-w-md mx-auto mt-8">
              <ProgressBar 
                value={5} 
                max={9} 
                label="30 天学习计划"
                size="lg"
              />
            </div>
            
            {/* CTA 按钮 */}
            <div className="flex gap-4 justify-center mt-8">
              <Button variant="primary" size="lg">
                查看学习进度
              </Button>
              <Button variant="secondary" size="lg">
                浏览项目
              </Button>
            </div>
          </div>
        </div>
      </section>
      
      {/* 统计卡片 */}
      <section className="max-w-7xl mx-auto px-6 py-16">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
          <Card className="p-6">
            <div className="text-text-secondary text-sm">完成任务</div>
            <div className="text-4xl font-bold text-text-primary mt-2">5/9</div>
            <Badge variant="success" size="sm" className="mt-3">
              55.6%
            </Badge>
          </Card>
          
          <Card className="p-6">
            <div className="text-text-secondary text-sm">知识卡片</div>
            <div className="text-4xl font-bold text-text-primary mt-2">8</div>
            <Badge variant="info" size="sm" className="mt-3">
              持续增长
            </Badge>
          </Card>
          
          <Card className="p-6">
            <div className="text-text-secondary text-sm">活跃项目</div>
            <div className="text-4xl font-bold text-text-primary mt-2">2</div>
            <Badge variant="warning" size="sm" className="mt-3">
              进行中
            </Badge>
          </Card>
          
          <Card className="p-6">
            <div className="text-text-secondary text-sm">学习天数</div>
            <div className="text-4xl font-bold text-text-primary mt-2">15</div>
            <Badge variant="success" size="sm" className="mt-3">
              坚持中
            </Badge>
          </Card>
        </div>
      </section>
    </div>
  )
}
```

---

### 步骤 6：更新导航栏（20 分钟）

**位置**：`src/src/components/layout/Header.tsx`（需创建）

**代码**：
```typescript
'use client'

import Link from 'next/link'
import { useState } from 'react'

export function Header() {
  const [theme, setTheme] = useState<'dark' | 'light'>('dark')
  
  const toggleTheme = () => {
    const next = theme === 'dark' ? 'light' : 'dark'
    setTheme(next)
    document.documentElement.setAttribute('data-theme', next)
  }
  
  return (
    <header className="sticky top-0 z-50 glass border-b border-border-default">
      <div className="max-w-7xl mx-auto px-6">
        <div className="flex items-center justify-between h-16">
          {/* Logo */}
          <Link href="/" className="text-xl font-semibold text-text-primary hover:text-primary transition-colors">
            努力学习的勇者
          </Link>
          
          {/* 导航 */}
          <nav className="flex items-center gap-8">
            <Link href="/learning" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
              学习进度
            </Link>
            <Link href="/projects" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
              项目展示
            </Link>
            <Link href="/knowledge" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
              知识网络
            </Link>
            <Link href="/about" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
              关于我
            </Link>
            
            {/* 主题切换 */}
            <button
              onClick={toggleTheme}
              className="p-2 rounded-lg hover:bg-dark-hover transition-colors"
              aria-label="切换主题"
            >
              {theme === 'dark' ? '🌙' : '☀️'}
            </button>
          </nav>
        </div>
      </div>
    </header>
  )
}
```

**然后在 layout.tsx 中使用**：
```typescript
import { Header } from '@/components/layout/Header'

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <Header />
        <main>{children}</main>
      </body>
    </html>
  )
}
```

---

### 步骤 7：测试和微调（20 分钟）

**操作**：
1. 启动开发服务器：`npm run dev`
2. 访问 `http://localhost:3000`
3. 检查以下内容：
   - [ ] 字体是否正确加载（Inter）
   - [ ] 配色是否符合 Linear 风格
   - [ ] 卡片悬浮效果是否流畅
   - [ ] 按钮样式是否正确
   - [ ] 进度条动画是否平滑
   - [ ] 导航栏毛玻璃效果是否生效
   - [ ] 主题切换是否工作

---

## ✅ 验收标准

完成后应该达到：

1. **视觉效果**
   - ✅ 深色背景 + 紫蓝主色
   - ✅ 卡片有悬浮效果
   - ✅ 渐变按钮和文字
   - ✅ 导航栏毛玻璃效果

2. **交互体验**
   - ✅ Hover 有平滑过渡
   - ✅ 点击有视觉反馈
   - ✅ 主题切换正常工作

3. **代码质量**
   - ✅ 组件可复用
   - ✅ 使用 CSS 变量
   - ✅ TypeScript 类型完整

---

## 🚨 注意事项

1. **创建目录**：`src/src/components/ui/` 和 `src/src/components/layout/` 目录需要先创建
2. **路径别名**：确保 `@/` 指向 `src/src/`
3. **CSS 顺序**：`design-system.css` 必须在 Tailwind 之后导入
4. **字体加载**：Inter 字体可能需要几秒钟加载，正常现象

---

## 📝 完成后报告

完成所有步骤后，请报告：
1. 哪些步骤成功完成
2. 遇到了什么问题
3. 最终效果截图或描述

---

**文档创建**：Claude Code  
**执行者**：Codex  
**预计完成时间**：2-3 小时
